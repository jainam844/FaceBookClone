import { Box } from '@mui/material';
import React from 'react';

const MarketPlace = () => {
  return (
    <Box sx={{ width: '80vw' }}>
    <div>
      <h2>MarketPlace</h2>
    </div>
    </Box>
  );
};

export default MarketPlace;
